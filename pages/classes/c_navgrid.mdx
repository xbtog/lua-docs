# c_navgrid

## Members

- `min_grid_pos:` [`vec3{:lua}`](/structs/math/vec3)
- `max_grid_pos:` [`vec3{:lua}`](/structs/math/vec3)
- `collision_map:` `number{:lua}`
- `sampled_heights:` `number{:lua}`
- `x_cell_count:` `number{:lua}`
- `y_cell_count:` `number{:lua}`
- `cell_size:` `number{:lua}`

## Functions

### get_collision

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| position | [`vec3{:lua}`](/structs/math/vec3) | +        |

```lua
local collision_flag = g_navgrid:get_collision(g_local.position)
```

### is_wall

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| position | [`vec3{:lua}`](/structs/math/vec3) | +        |

```lua /is_wall/
local point_is_in_wall = g_navgrid:is_wall(g_local.position)
```

### is_bush

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| position | [`vec3{:lua}`](/structs/math/vec3) | +        |

```lua /is_bush/
local in_bush = g_navgrid:is_bush(g_local.position)
```

### is_building

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| position | [`vec3{:lua}`](/structs/math/vec3) | +        |

```lua /is_building/
local in_building = g_navgrid:is_building(g_local.position)
```

### is_river

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| position | [`vec3{:lua}`](/structs/math/vec3) | +        |

```lua /is_river/
local in_river = g_navgrid:is_river(g_local.position)
```

### is_bush

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| position | [`vec3{:lua}`](/structs/math/vec3) | +        |

```lua
local is_bush = g_navgrid:is_bush(g_local.position)
```

### is_river

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| position | [`vec3{:lua}`](/structs/math/vec3) | +        |

```lua
local is_river = g_navgrid:is_river(g_local.position)
```

### is_building

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| position | [`vec3{:lua}`](/structs/math/vec3) | +        |

```lua
local is_building = g_navgrid:is_building(g_local.position)
```

### get_height

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| position | [`vec3{:lua}`](/structs/math/vec3) | +        |

```lua
local height = g_navgrid:get_height(g_local.position)
```
