# c_threading

## Functions

### is_feature_thread

Check if current callback is running on the feature thread.

```lua
if g_threading:is_feature_thread() then
    print("is feature thread")
end
```

### is_render_thread

Check if current callback is running on the render thread.
