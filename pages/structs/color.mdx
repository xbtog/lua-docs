# color

## Members

- `r:` `number{:lua}`
- `g:` `number{:lua}`
- `b:` `number{:lua}`
- `a:` `number{:lua}`

## Constructors

```lua
local white = color.new(255, 255, 255)
local white_with_alpha = color.new(255, 255, 255, 150)
local color_from_hex = color.new("#ffffff")

local BLACK = color.black()
local WHITE = color.white()
local RED = color.red()
local GREEN = color.green()
local BLUE = color.blue()
```
