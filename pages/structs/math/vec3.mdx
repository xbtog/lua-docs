# vec3

## Members

- `x:` `number{:lua}`
- `y:` `number{:lua}`
- `z:` `number{:lua}`

## Operators

### `+{:lua}`

### `-{:lua}`

### `*{:lua}`

### `/{:lua}`

## Functions

### length

| Argument | Type           | Required |
| -------- | -------------- | -------- |
| returns  | `number{:lua}` |          |

```lua
vec:length()
```

### length2d

| Argument | Type           | Required |
| -------- | -------------- | -------- |
| returns  | `number{:lua}` |          |

```lua
vec:length2d()
```

### dist_to

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| other    | [`vec3{:lua}`](/structs/math/vec3) | +        |
| returns  | `number{:lua}`                     |          |

```lua
vec3.new(1, 1):dist_to(vec3.new(2, 2))
```

### dot

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| other    | [`vec3{:lua}`](/structs/math/vec3) | +        |
| returns  | `number{:lua}`                     |          |

```lua
vec:dot(other)
```

### cross

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| other    | [`vec3{:lua}`](/structs/math/vec3) | +        |
| returns  | `number{:lua}`                     |          |

```lua
vec:cross(other)
```

### normalized

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| returns  | [`vec3{:lua}`](/structs/math/vec3) |          |

```lua
vec:normalized()
```

### angle_between

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| other    | [`vec3{:lua}`](/structs/math/vec3) | +        |
| returns  | `number{:lua}`                     |          |

```lua
vec:angle_between(other)
```

### rotated

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| angle    | `number{:lua}`                     | +        |
| returns  | [`vec3{:lua}`](/structs/math/vec3) |          |

```lua
vec:rotated(angle)
```

### extend

| Argument | Type                               | Required |
| -------- | ---------------------------------- | -------- |
| to       | [`vec3{:lua}`](/structs/math/vec3) | +        |
| distance | `number{:lua}`                     | +        |
| returns  | [`vec3{:lua}`](/structs/math/vec3) |          |

```lua
vec:extend(to, distance)
```

### to_screen

Get position on screen

| Argument | Type                                      | Required |
| -------- | ----------------------------------------- | -------- |
| returns  | [`vec2 \| nil{:lua}`](/structs/math/vec2) |          |

```lua
vec:to_screen()
```

### to_minimap

Get position on minimap

| Argument | Type                                      | Required |
| -------- | ----------------------------------------- | -------- |
| returns  | [`vec2 \| nil{:lua}`](/structs/math/vec2) |          |

```lua
vec:to_minimap()
```
